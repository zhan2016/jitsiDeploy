import _classCallCheck from 'babel-runtime/helpers/classCallCheck';
import _possibleConstructorReturn from 'babel-runtime/helpers/possibleConstructorReturn';
import _inherits from 'babel-runtime/helpers/inherits';
import PropTypes from 'prop-types';
import { PureComponent } from 'react';
import ItemShape from './ItemShape';

/** ************************************************************************************************
  This file exists so that we have a component we can pass the @atlaskit/readme Props component
**************************************************************************************************/

var GroupShape = function (_PureComponent) {
  _inherits(GroupShape, _PureComponent);

  function GroupShape() {
    _classCallCheck(this, GroupShape);

    return _possibleConstructorReturn(this, _PureComponent.apply(this, arguments));
  }

  return GroupShape;
}(PureComponent);

GroupShape.propTypes = {
  heading: PropTypes.string,
  items: PropTypes.arrayOf(PropTypes.shape(ItemShape.propTypes))
};
export default GroupShape;