import { akGridSizeUnitless } from '@atlaskit/util-shared-styles';

export var modalShadowInnerSize = 2; // 2px
export var modalBorderRadius = akGridSizeUnitless / 2; // 4px
export var WIDTH_ENUM = {
  values: ['small', 'medium', 'large', 'x-large'],
  widths: {
    small: akGridSizeUnitless * 50, // 400px
    medium: akGridSizeUnitless * 75, // 600px
    large: akGridSizeUnitless * 100, // 800px
    'x-large': akGridSizeUnitless * 121 },
  defaultValue: 'medium'
};
export var modalAnimationDuration = 0.25;